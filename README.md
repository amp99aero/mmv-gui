A GUI wrapper for the mmv command, built in Python. Only works on linux*.

Required python modules:
  numpy
  PySimpleGUIQt

Other requirements:
  mmv

* The program may work in Windows by substituting PySimpleGUIQt with PySimpleGUI, but is not tested.
